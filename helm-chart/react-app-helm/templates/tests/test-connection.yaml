apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "react-app-helm.fullname" . }}-test-connection"
  labels:
    {{- include "react-app-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "react-app-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
